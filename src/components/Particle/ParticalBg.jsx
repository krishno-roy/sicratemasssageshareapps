import React from "react";
import Particles from "react-tsparticles"; 
import ParticalConfig from "./ParticalConfig";

const ParticalBg = () => {
  return <Particles options={ParticalConfig} />;
};

export default ParticalBg;
